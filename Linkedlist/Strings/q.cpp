#include<iostream>
#include<vector>
using namespace std;
int maximumnumberofnumber(string st){
    vector<int> hash(256,-1);
    int n=st.length();
    int left=0,right=0,maxLen=0;
    while(right<n){
        if(hash[st[right]]!=-1 && hash[st[right]]>l)

    }
}
int main(){
    string st="abcabcdcc";

}